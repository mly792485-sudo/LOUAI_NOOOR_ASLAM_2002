# تشغيل ونشر نور الإسلام

## إصلاح مستشار نور الإسلام

مستشار الذكاء الاصطناعي لا يتصل مباشرة من التطبيق إلى OpenAI. التطبيق يتصل بخادم `server.ts`، والخادم وحده يحتفظ بالمفتاح السري. لذلك لا تضع مفتاح OpenAI داخل Flutter أو داخل مستودع GitHub.

### نشر الخادم

يمكن نشر المشروع على Render أو Railway أو أي استضافة Node.js تدعم HTTPS. أمر البناء والتشغيل:

```bash
npm ci
npm run build
npm start
```

أضف متغيرات البيئة في لوحة الاستضافة:

```text
OPENAI_API_KEY=ضع_مفتاحك_هنا_في_لوحة_الاستضافة_فقط
OPENAI_MODEL=gpt-4o-mini
PORT=10000
```

اختبر النشر:

```bash
curl https://YOUR-DOMAIN/health
```

ينبغي أن يرجع الخادم JSON فيه `ok: true`. لا تستخدم رابطًا يبدأ بـ `http://` في النسخ النهائية.

## GitHub Secrets

في المستودع افتح **Settings → Secrets and variables → Actions → New repository secret** وأضف:

| الاسم | القيمة | الاستخدام |
|---|---|---|
| `VITE_API_BASE_URL` | رابط HTTPS العام للخادم، مثل `https://noor-api.example.com` | حقن رابط الخادم في بناء الويب وAndroid وiPhone |
| `OPENAI_API_KEY` | مفتاح OpenAI السري | يُستخدم فقط إذا كان GitHub Actions هو الذي ينشر الخادم؛ لا تطبعه في السجلات |

حاليًا ملفات البناء تستخدم `VITE_API_BASE_URL`، بينما Flutter يقبل `ASSISTANT_BASE_URL` أو `VITE_API_BASE_URL`. لا تضع قيمة المفتاح داخل ملفات `.env` أو Dart أو TypeScript المرفوعة إلى GitHub.

## البناء

### Android وiPhone عبر GitHub Actions

بعد إضافة `VITE_API_BASE_URL` شغّل workflow من تبويب **Actions**. ينتج workflow Android ملفات APK وAAB غير موقعة، وينتج workflow iPhone ملف IPA غير موقع للتحقق. التثبيت الفعلي على iPhone أو الرفع إلى App Store يتطلب شهادة Apple وProvisioning Profile، كما أن APK/AAB النهائي للنشر يحتاج توقيع Android.

### Flutter

```bash
cd flutter_app
flutter pub get
flutter build apk --release --dart-define=ASSISTANT_BASE_URL=https://YOUR-DOMAIN
flutter build ipa --release --dart-define=ASSISTANT_BASE_URL=https://YOUR-DOMAIN
```

النسخة الحالية من Flutter تتضمن المسار الأساسي للمساعد والمصحف، بينما نسخة Capacitor/React هي التي تحتوي على بقية واجهة التطبيق والمكتبة والإذاعات الحالية. لا تحذف أيًا منهما قبل اكتمال نقل الواجهات إلى Flutter.

## التنبيهات التي تم تثبيتها

تم حذف إشعار «بعد قليل» وإشعار «باقي خمس دقائق على الإقامة» من جدولة Android وiPhone والويب. المتبقي هو إشعار أذان دخول الوقت وإشعار الإقامة فقط للصلوات الخمس. الشروق ليس له أذان. بقيت تذكيرات أذكار الصباح والمساء والنوم والوتر وقيام الليل وسورة الكهف يوم الجمعة كما هي.

## مواقيت الصلاة

التطبيق يجلب مواقيت اليوم من API مع المدينة والدولة وطريقة الحساب، ويحدّثها حسب التاريخ والموقع. في البحرين توجد خوارزمية منفصلة بملف المصدر الرسمي المعلن داخل التطبيق. لضمان التطابق مع جدول وزارة أو إدارة أوقاف محددة، يجب توفير feed رسمي قابل للقراءة آليًا أو مراجعة فروق طريقة الحساب والضبط اليدوي من الجهة الرسمية.

## روابط المشروع

- مستودع GitHub: https://github.com/mly792485-sudo/BODYBHQQQ
- فحص الخادم: `https://YOUR-DOMAIN/health`
- مستشار الأسئلة: `https://YOUR-DOMAIN/api/ai/qa`

لا يمكن إنشاء رابط APK أو IPA جاهز من داخل الجهاز المحلي هنا دون أدوات Android/Xcode ومفاتيح توقيع، لكن ملفات GitHub Actions في هذا المشروع تبنيها تلقائيًا وتضع النتائج في Artifacts بعد نجاح التشغيل.

## قائمة تحقق قبل الإطلاق

1. اختبر `/health` ثم `/api/ai/qa` من الخادم.
2. تأكد أن `VITE_API_BASE_URL` لا ينتهي بمسار إضافي ولا يحتوي على مسافة.
3. اطلب إذن الإشعارات والموقع على الجهاز.
4. اختبر صلاة واحدة في وضع تجريبي، وتحقق من وجود إشعارين فقط: الأذان والإقامة.
5. راجع Artifact الخاص بالمنصة، ثم وقّع الإصدار بمفاتيحك خارج المستودع.

---

> تنبيه: مستشار نور الإسلام أداة بحث وشرح، وليس مفتيًا مستقلًا. في الطلاق والمواريث والمعاملات والفتاوى الخاصة يُرجع إلى عالم موثوق أو جهة إفتاء رسمية.

